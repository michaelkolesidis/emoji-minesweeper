export default function ThemeButton(mainEmoji) {
  // Button
  const themeButton = document.createElement("div");
  themeButton.className = `emoji-button`;
  themeButton.innerHTML = mainEmoji;

  // Functionality
  // In main.js

  return themeButton;
}
